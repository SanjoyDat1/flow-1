# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Workspace runtime artifacts (regenerated by the pipeline)
output/*.json
failed/*.txt
failed/*.error.txt
processed/*.txt
queue/*.json

# Incoming transcripts are transient, but keep the example tracked so the
# repo is runnable out-of-the-box (negation must follow the ignore rule).
transcripts/*.txt
!transcripts/20260529_104800_sample_meeting.txt
